# Java
Fundamentos e Sintaxe Java → JVM (Java Virtual Machine) e JRE/JDK → Tipos Primitivos e Wrappers → Estruturas de Controle (If, Switch, Loops) → Arrays e Matrizes → Programação Orientada a Objetos (Classes, Atributos, Métodos) → Encapsulamento (Getters/Setters) → Herança e Polimorfismo → Abstração e Interfaces → Tratamento de Exceções (Try-Catch-Finally) → Java Collections Framework (ArrayList, HashMap, HashSet) → Trabalhando com Generics → Streams API e Lambdas (Java 8+) → Maven ou Gradle (Gerenciadores de Dependências) → Injeção de Dependência e Inversão de Controle → Acesso a Dados com JDBC e Hibernate (ORM) → Spring Boot (O padrão para Bancos e Multinacionais) → Criação de APIs RESTful → Segurança e JWT (JSON Web Tokens) → Testes Unitários com JUnit → Conceitos de CI/CD e Docker para Java → Introdução à API do Forge/Fabric (Para Mods de Minecraft) → Manipulação de JSON e NBT Tags (Minecraft Data) → Event Listeners e Registro de Itens/Blocos Customizados.
